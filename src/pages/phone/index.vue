<template><view :data-skin="scenicSkin" class="visual-page safe"><SceneBackdrop :src="backgrounds.engagement" :shade=".65"/><TopBar title="手机" eyebrow="PRIVATE LIFE" @back="back" @settings="settings"/><view class="visual-content"><PhoneScene :continue-label="inStory?'收起手机，继续剧情':'收起手机'" @continue="complete"/></view></view></template><script setup lang="ts">
import {goBack} from '../../utils/navigation'
import {computed} from 'vue';import {gameStore} from '../../store/game';import {backgrounds} from '../../data/backgrounds';import TopBar from '../../components/TopBar.vue';import SceneBackdrop from '../../components/common/SceneBackdrop.vue';import PhoneScene from '../../components/phone/PhoneScene.vue';const inStory=computed(()=>gameStore.getNode().page==='phone');function back(){goBack()}function settings(){uni.navigateTo({url:'/pages/settings/index'})}function complete(){if(inStory.value){gameStore.setNode(gameStore.getNode().next||'V_M01');uni.reLaunch({url:'/pages/play/index'})}else back()}</script>
